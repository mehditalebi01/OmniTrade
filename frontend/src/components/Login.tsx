import{FormEvent,useState}from'react';import{Alert,Box,Button,Card,CardContent,Stack,TextField,Typography}from'@mui/material';import AutoGraphIcon from'@mui/icons-material/AutoGraph';import{login}from'../api';
export default function Login({onLogin}:{onLogin:()=>void}){const[username,setUsername]=useState('demo'),[password,setPassword]=useState('demo'),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');try{await login(username,password);onLogin()}catch(err){setError(err instanceof Error?err.message:'Login failed')}}return<Box sx={{minHeight:'100vh',display:'grid',placeItems:'center',background:'radial-gradient(circle at top,#1d2b55,#08101f 55%)'}}><Card sx={{width:420,maxWidth:'92vw'}}><CardContent sx={{p:4}}><Stack component="form" spacing={2.5} onSubmit={submit}><AutoGraphIcon color="secondary" sx={{fontSize:48}}/><Box><Typography variant="h4" fontWeight={800}>OmniTrade AI</Typography><Typography color="text.secondary">Secure local workflow studio</Typography></Box>{error&&<Alert severity="error">{error}</Alert>}<TextField label="Username" value={username} onChange={e=>setUsername(e.target.value)}/><TextField label="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><Button type="submit" size="large" variant="contained">Sign in</Button><Typography variant="caption" color="text.secondary">Demo: demo / demo. No broker connection is available.</Typography></Stack></CardContent></Card></Box>}
